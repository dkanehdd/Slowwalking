<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="member.AdminSitterImpl">

<!-- 공지사항 수정 처리 -->
	<update id="permission" parameterType="String">
		UPDATE sitterMember 
		SET  permission='T'
		WHERE sitter_id=#{0}
	</update>


	
</mapper>